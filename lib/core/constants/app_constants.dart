/// Application-wide constants.
class AppConstants {
  AppConstants._();

  static const String appName = 'PigWeigh';

  /// TFLite model asset path (relative to `assets/models/`)
  static const String weightModelAsset = 'assets/models/pig_weight.tflite';
}
